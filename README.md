# CryPD
CryPD is a lightweight CLI password manager written in Python. It securely stores credentials in an encrypted vault using PBKDF2-HMAC-SHA256 key derivation and AES-GCM encryption. Manage, search, add, and delete passwords locally with a master password.
